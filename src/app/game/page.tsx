import { DoomGame } from "~/app/game/_components/doom-game";

/**
 * Game route — renders the Three.js Doom clone in a client-only canvas.
 */
export default function GamePage() {
  return <DoomGame />;
}
